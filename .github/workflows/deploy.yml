name: Deploy Nahj al-Balagha UI

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build application
        run: |
          npm ci
          npm run build

      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.2.2
        with:
          host: ${{ secrets.SFTP_HOST }}
          username: ${{ secrets.SFTP_USER }}
          password: ${{ secrets.SFTP_PASS }}
          script_stop: true
          script: |
            set -e

            echo "========================================"
            echo "Deploying Nahj al-Balagha UI"
            echo "========================================"

            sudo -u nahjui -H bash -lc '
              set -e

              source ~/.nvm/nvm.sh
              nvm use 22

              cd /home/nahjui/app

              echo "Pulling latest code..."
              git fetch origin
              git reset --hard origin/main

              echo "Installing dependencies..."
              npm ci

              echo "Building Next.js..."
              npm run build
            '

            echo "Restarting Next.js..."
            sudo -n systemctl restart nahj-ui

            echo "Checking Next.js..."
            sudo -n systemctl is-active --quiet nahj-ui

            echo "Testing local application..."
            curl -f http://127.0.0.1:3000/ > /dev/null

            echo "========================================"
            echo "Deployment successful"
            echo "========================================"